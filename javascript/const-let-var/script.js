var globalVariable = `It's a global level scope variable.`;
let blockVariable = `It's a block level scope variable.`;
const constantValue = `It's a constant value which can't be changed later.`

let space = ` `;

console.log(globalVariable + space + blockVariable + space + constantValue);